# On EigenCompute this is injected at runtime by KMS. Do NOT hardcode real secrets.
MNEMONIC=

# Optional metadata placeholders shown in the report.
APP_ADDRESS=<app-address-placeholder>
DOCKER_IMAGE_DIGEST=<docker-image-digest-placeholder>

# Optional runtime tuning
N_SHOT=0
LM_EVAL_LIMIT=
